@model IEnumerable<CodeCraft.Data.Models.Inquiry>

@{
    ViewData["Title"] = "All Contact Inquiries";
}

<div class="row page-titles mx-0">
    <div class="col-sm-6 p-md-0">
        <div class="welcome-text">
            <h4>All Inquiries</h4>
        </div>
    </div>
    <div class="col-sm-6 p-md-0 justify-content-sm-end mt-2 mt-sm-0 d-flex">
        <ol class="breadcrumb">
            <li class="breadcrumb-item">
                <a asp-controller="Dashboard" asp-action="Index">
                    Dashboard
                </a>
            </li>
            <li class="breadcrumb-item active">
                <a asp-action="Index">
                    Inquiries
                </a>
            </li>
            <li class="breadcrumb-item active">
                All
            </li>
        </ol>
    </div>
</div>

<div class="row">
    <div class="col-lg-12">
        <div class="row tab-content">
            <div id="list-view" class="tab-pane fade active show col-lg-12">
                <div class="card">
                    <div class="card-header">
                        <h4 class="card-title">All Inquiries</h4>
                        <a asp-action="Create" class="btn btn-primary">
                            <i class="la la-plus-circle"></i> Add Inquiry
                        </a>
                    </div>
                    <div class="card-body">
                        <div class="table-responsive">
                            <table id="example3" class="display" style="min-width: 845px">
                                <thead>
                                    <tr>
                                        <th>#</th>
                                        <th>@Html.DisplayNameFor(model => model.Email)</th>
                                        <th>@Html.DisplayNameFor(model => model.Message)</th>
                                        <th>Action</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (Inquiry inquiry in Model)
                                    {
                                        <tr>
                                            <td>
                                                @Html.DisplayFor(modelItem => inquiry.Id)
                                            </td>
                                            <td>
                                                <a asp-action="Details" asp-route-id="@inquiry.Id">
                                                    @Html.DisplayFor(modelItem => inquiry.Email)
                                                </a>
                                            </td>
                                            <td>
                                                @Html.DisplayFor(modelItem => inquiry.Message)
                                            </td>
                                            <td>
                                                <a asp-action="Details" asp-route-id="@inquiry.Id" class="btn btn-sm btn-primary">
                                                    <i class="la la-eye"></i>
                                                </a>
                                                <a asp-action="Edit" asp-route-id="@inquiry.Id" class="btn btn-sm btn-primary">
                                                    <i class="la la-pencil"></i>
                                                </a>
                                                <a asp-action="Delete" asp-route-id="@inquiry.Id" class="btn btn-sm btn-danger">
                                                    <i class="la la-trash-o"></i>
                                                </a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>